/**
* Autogenerated Header File for EvioFileReader
* \\file EvioFileReader.h
* EvioFileReader is a class used for reading CLAS12 evio data format.
* It provides methods to to read any kind of structures from evio, but
* high level interface is adapted specifically for CLAS12.
* \\author G.Gavalian
* \\date 2012-07-14
*/

#ifndef __EVIOFILEREADER__
#define __EVIOFILEREADER__

#include <iostream>
#include <string>
#include <cmath>
#include <vector>
#include <ctime>
#include "EvioDataEvent.h"
#include "EvioDataBank.h"
#include "common_std.h"

#ifndef MAXEVIOBUF
#define MAXEVIOBUF   400000
#endif

using namespace std;

//! A Brief Description
/*
 * A detailed description
 */

class EvioFileReader {

private:
    
    long    numberOfEventsRead;
    long  double readTimeTotalMS;
    
    int       evioFileHandle;
    uint32_t  buffer[MAXEVIOBUF];
    string    currentFileName;
    EvioDataEvent  dataEvent;
    uint32_t fileEventsCount;
    uint32_t version;
public:

/** Default Constructor */
EvioFileReader();
/** Default Destructor */
~EvioFileReader();

 void open(const char *filename);
 void close();
 
 bool next();
 int               getEntries();
 vector<double>   *getDoubleVector(int tag, int num);
 vector<int32_t>  *getIntVector(int tag, int num);
 void              getBank(EvioDataBank& bank,int tag, int num = 0);
 string            getDataFormat(int tag, int num);
 int               readEvent(int eventnum);

 void  getEvent(EvioDataEvent &event);
 
 EvioDataEvent &getEvent();

 //void getEvent();
 /** An operator = if one is needed */
 /* const TEvioFileReader &operator=(const TEvioFileReader &obj); */

};
#endif
